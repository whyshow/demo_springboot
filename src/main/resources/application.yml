server:
  # 端口
  port: 8080
  servlet:
    # 首页路径
    context-path: /
#数据库配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demo?serverTimezone=UTC
    username: root
    password: swzhang3
